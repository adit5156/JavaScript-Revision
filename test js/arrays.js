let arr = [1,6,8,3];
// console.log(arr);
// console.log(arr[1]);

// console.log(arr.length);

// for(let i = 0; i<arr.length; i++) {
//     console.log(arr[i]);
// }

console.log(arr);
arr.push(10); // adds element to the last position in an array
console.log(arr);
arr.pop();  // removes the last element of the array
console.log(arr);

arr.shift();    // removes the first index of the array
console.log(arr);
arr.unshift(3,10); // adds elements in the beginning of an array
console.log(arr)